name: Check Project

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: macos-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Show files
      run: |
        pwd
        ls -la
        find . -name "*.xcodeproj"
